# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Node / npm (dev tooling only — extension itself has zero runtime deps)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Test / coverage output
coverage/
.nyc_output/
*.lcov

# Build artifacts
dist/
*.zip
*.xpi
*.tar.gz
web-ext-artifacts/

# Secrets (should never exist, but safety net)
*.env
.env.*
secrets.json
.web-ext-api-key

# Competitive analysis and business docs (do not belong in repo)
*.pdf

# Bench output — per-run results are not committed; baselines are.
bench/results-*.json
